# R helper skeletons

Source `activity_common.R` first, then source method-specific files.

These helpers are drafts intended for reuse in project scripts. They avoid project-specific paths and should be wrapped by analysis-specific scripts that set output directories, object paths, and biological contrasts.
