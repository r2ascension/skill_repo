# 说明

- `network_shuodao_writing_catalog.md` 和 `network_shuodao_writing_catalog.json` 由 `scripts/refresh_catalog.py` 生成。
- 当前 skill 约定通过 `opencli bilibili` 实时抓取数据，不依赖手工维护的清单。
- 如果用户只要一份快速答案，优先读取 `network_shuodao_writing_catalog.md`。
- 如果用户要继续分析、重分组或导出结构化结果，再读取 `network_shuodao_writing_catalog.json`。
